{# ┌─────────────────────────────────────────────────────────┐
   │ MOLECULE › List Value                                   │
   │ Value lists with lockup items and layout variants      │
   └─────────────────────────────────────────────────────────┘ #}

{#
  Props:
    - options.id (string): List value identifier
    - options.datas (array): List value configuration data
    - options.lockupDatas (array): Lockup molecule data
    - options.variant (string): Override layout variant ("default" or "horizontal")

  Usage — Summon HAT Components Wisely:
    {% from "02-molecules/list-value.njk" import renderListValue %}

    {{ renderListValue({
        id: "demoList1",
        datas: molecules["list-value"].listValues,
        lockupDatas: molecules["lock-up"].lockUps
    }) }}

    {{ renderListValue({
        id: "demoList1",
        datas: molecules["list-value"].listValues,
        lockupDatas: molecules["lock-up"].lockUps,
        variant: "horizontal"
    }) }}

  @created 2025-01-15
#}

{% from "02-molecules/lock-up.njk" import renderLockup %}

{% macro renderListValue(options) %}

  {# ━━━━━━━━━━━━━━━━━━━━━━━━━━
    1. Validate Props
  ━━━━━━━━━━━━━━━━━━━━━━━━━ #}
  {% if not options.datas %}
    <span class="error" role="alert">Error: Missing required prop (datas)</span>
  {% elif not options.id %}
    <span class="error" role="alert">Error: Missing required prop (id)</span>
  {% else %}
    {# Find list value using universal filter #}
    {% set listData = options.datas | findByName(options.id) %}

    {# Render list value or error message #}
    {% if not listData %}
      <span class="error" role="alert">List value not found: {{ options.id }}</span>
    {% else %}

      {# ━━━━━━━━━━━━━━━━━━━━━━━━━━
        2. Compute Final Values
      ━━━━━━━━━━━━━━━━━━━━━━━━━ #}
      {% set listId = listData.id | default(options.id) %}
      {% set variant = options.variant | default(listData.variant) | default('default') %}
      {% set items = listData.items | default([]) %}

      {# ━━━━━━━━━━━━━━━━━━━━━━━━━━
        3. Tailwind Classes
        Index:
        3.1 Wrapper Classes
        3.2 Item Classes
      ━━━━━━━━━━━━━━━━━━━━━━━━━ #}

      {# --------------------------
          3.1 Wrapper Classes
          Description: Layout by variant, Spacing
        --------------------------#}
      {% set containerClasses = "" %}

      {% if variant == 'horizontal' %}
        {% set containerClasses = "flex flex-row gap-[var(--spacing-inline-large)]" %}
      {% else %}
        {% set containerClasses = "flex flex-col gap-[var(--spacing-stack-medium)]" %}
      {% endif %}

      {# --------------------------
          3.2 Item Classes
          Description: Layout
        --------------------------#}
      {% set itemClasses = "" %}

      {# ━━━━━━━━━━━━━━━━━━━━━━━━━━
        4. Render HTML
      ━━━━━━━━━━━━━━━━━━━━━━━━━ #}
      <div
        class="{{ containerClasses }}"
        data-list-value-id="{{ listId }}"
        data-list-value-type="list-value"
        data-list-value-variant="{{ variant }}"
      >
        {% for item in items %}
          <div
            class="{{ itemClasses }}"
            data-list-value-item="true"
          >
            {# Render lockup #}
            {{ renderLockup({
                name: item.lockupName,
                datas: options.lockupDatas
            }) }}
          </div>
        {% endfor %}
      </div>
    {% endif %}
  {% endif %}
{% endmacro %}

{#
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
May your bugs be forever exiled to the shadow realm ✦
HAT · 2025
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#}
