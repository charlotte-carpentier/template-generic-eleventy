{# ┌─────────────────────────────────────────────────────────┐
   │ MOLECULE › List Value                                   │
   │ Value lists with lockup items and layout variants      │
   └─────────────────────────────────────────────────────────┘ #}

{#
  Props:
    - options.id (string): List value identifier
    - options.datas (array): List value configuration data
    - options.lockupDatas (array): Lockup molecule data

  Usage — Summon HAT Components Wisely:
    {% from "02-molecules/list-value.njk" import renderListValue %}

    {{ renderListValue({
        id: "demoList1",
        datas: molecules["list-value"].listValues,
        lockupDatas: molecules["lock-up"].lockUps
    }) }}

  @created 2025-01-15
#}

{% from "02-molecules/lock-up.njk" import renderLockup %}

{% macro renderListValue(options) %}
  {# Validate required props #}
  {% if not options.datas %}
    <span class="error" role="alert">Error: Missing required prop (datas)</span>
  {% elif not options.id %}
    <span class="error" role="alert">Error: Missing required prop (id)</span>
  {% else %}
    {# Find list value using universal filter #}
    {% set listData = options.datas | findByName(options.id) %}

    {# Render or error #}
    {% if not listData %}
      <span class="error" role="alert">List value not found: {{ options.id }}</span>
    {% else %}
      <div
        data-list-value-id="{{ listData.id }}"
        data-list-value-type="list-value"
      >
        {% for item in listData.items %}
          <div data-list-value-item="true">
            {# Render lockup #}
            {{ renderLockup({
                name: item.lockupName,
                datas: options.lockupDatas
            }) }}
          </div>
        {% endfor %}
      </div>
    {% endif %}
  {% endif %}
{% endmacro %}

{#
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
May your bugs be forever exiled to the shadow realm ✦
HAT · 2025
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#}
