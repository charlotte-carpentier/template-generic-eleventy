{# ┌─────────────────────────────────────────────────────────┐
   │ ORGANISM › Tab Bar                                      │
   │ Navigation tab bar with active state management         │
   └─────────────────────────────────────────────────────────┘ #}

{#
  Props:
    - options.name (string): Tab bar identifier
    - options.datas (array): Tab bar configuration data
    - options.linkDatas (array): Link atom data

  Usage — Summon HAT Components Wisely:
    {% from "03-organisms/tab-bar.njk" import renderTabBar %}

    {{ renderTabBar({
        name: "demoTabBar1",
        datas: organisms["tab-bar"].tabBars,
        linkDatas: atoms.link.links
    }) }}

  @created 2025-01-15
#}

{% macro renderTabBar(options) %}
  {# Import sub-component #}
  {% from "01-atoms/link.njk" import renderLink %}

  {# ━━━━━━━━━━━━━━━━━━━━━━━━━━
    1. Validate Props
  ━━━━━━━━━━━━━━━━━━━━━━━━━ #}
  {% if not options.datas %}
    <span class="error" role="alert">Error: Missing required prop (datas)</span>
  {% elif not options.name %}
    <span class="error" role="alert">Error: Missing required prop (name)</span>
  {% else %}
    {# Find tab bar using universal filter #}
    {% set tabBarData = options.datas | findByName(options.name) %}

    {# Render tab bar or error message #}
    {% if not tabBarData %}
      <span class="error" role="alert">Tab bar not found: {{ options.name }}</span>
    {% else %}

      {# ━━━━━━━━━━━━━━━━━━━━━━━━━━
        2. Compute Final Values
      ━━━━━━━━━━━━━━━━━━━━━━━━━ #}
      {% set tabBarName = tabBarData.name | default(options.name) %}
      {% set items = tabBarData.items | default([]) %}
      {% set ariaLabel = tabBarData.ariaLabel | default('Navigation tabs') %}

      {# ━━━━━━━━━━━━━━━━━━━━━━━━━━
        3. Tailwind Classes
        Index:
        3.1 Wrapper Classes
        3.2 List Classes
        3.3 Item Classes
        3.4 Link Override Classes
      ━━━━━━━━━━━━━━━━━━━━━━━━━ #}

      {# --------------------------
          3.1 Wrapper Classes
          Description: Layout
        --------------------------#}
      {% set navClasses = "flex" %}

      {# --------------------------
          3.2 List Classes
          Description: Layout horizontal, Spacing
        --------------------------#}
      {% set listClasses = "flex gap-[var(--spacing-6)]" %}

      {# --------------------------
          3.3 Item Classes
          Description: Layout
        --------------------------#}
      {% set listItemClasses = "flex" %}

      {# --------------------------
          3.4 Link Override Classes
          Description: Colors, States, Pseudo-elements for tabs
        --------------------------#}

      {% set linkBaseClasses = "[&_a]:relative [&_a]:px-[var(--spacing-2)] [&_a]:py-[var(--spacing-2)] [&_a]:pb-[var(--spacing-2)] [&_a]:typography-body-regular [&_a]:text-[var(--color-global-default)] [&_a]:no-underline [&_a]:transition-colors" %}

      {% set linkHoverClasses = "[&_a:hover]:text-[var(--color-global-contrast-light-01)] [&_a:hover]:after:content-['•'] [&_a:hover]:after:absolute [&_a:hover]:after:bottom-0 [&_a:hover]:after:translate-y-[8px] [&_a:hover]:after:left-1/2 [&_a:hover]:after:-translate-x-1/2" %}

      {% set linkFocusClasses = "[&_a:focus]:outline [&_a:focus]:outline-2 [&_a:focus]:outline-[var(--color-semantic-information-focus)] [&_a:focus]:rounded-[var(--radius-default)]" %}

      {% set linkActiveClasses = "[&_a[aria-selected='true']]:text-[var(--color-global-contrast-light-02)] [&_a[aria-selected='true']]:cursor-not-allowed [&_a[aria-selected='true']]:pointer-events-none [&_a[aria-selected='true']]:after:content-[''] [&_a[aria-selected='true']]:after:absolute [&_a[aria-selected='true']]:after:bottom-[calc(var(--spacing-0-5))] [&_a[aria-selected='true']]:after:left-1/2 [&_a[aria-selected='true']]:after:-translate-x-1/2 [&_a[aria-selected='true']]:after:h-1 [&_a[aria-selected='true']]:after:w-4 [&_a[aria-selected='true']]:after:bg-[var(--color-global-contrast-light-02)] [&_a[aria-selected='true']]:after:rounded-[var(--radius-default)]" %}

      {# ━━━━━━━━━━━━━━━━━━━━━━━━━━
        4. Render HTML
      ━━━━━━━━━━━━━━━━━━━━━━━━━ #}
      <nav
        class="{{ navClasses }}"
        role="tablist"
        aria-label="{{ ariaLabel }}"
        data-tabbar-name="{{ tabBarName }}"
        data-tabbar-type="tab-bar"
      >
        <ul
          class="{{ listClasses }} {{ linkBaseClasses }} {{ linkHoverClasses }} {{ linkFocusClasses }} {{ linkActiveClasses }}"
          data-tabbar-list
        >
          {% for item in items %}
            <li
              class="{{ listItemClasses }}"
              role="presentation"
              data-tabbar-item
            >
              {{ renderLink({
                  name: item.linkName,
                  datas: options.linkDatas,
                  active: item.active,
                  role: "tab",
                  ariaSelected: item.active
              }) }}
            </li>
          {% endfor %}
        </ul>
      </nav>
    {% endif %}
  {% endif %}
{% endmacro %}

{#
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
May your bugs be forever exiled to the shadow realm ✦
HAT · 2025
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#}
